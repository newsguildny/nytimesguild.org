// eslint-disable-next-line camelcase
import { Public_Sans, Crimson_Pro } from "next/font/google";

export const publicSans = Public_Sans({
  display: "optional",
  subsets: ["latin"],
});

export const crimsonPro = Crimson_Pro({
  display: "optional",
  subsets: ["latin"],
});
